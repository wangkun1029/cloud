server:
  port: 9001

spring:
  # 数据源配置
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
#        不可以带http
        server-addr: 192.168.71.30:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'